###############################################################
# Function : clean_price
# Purpose  : Convert Price column to numeric
###############################################################

clean_price <- function(price){
  
  price <- gsub("\\$", "", price)
  
  price <- as.numeric(price)
  
  return(price)
  
}